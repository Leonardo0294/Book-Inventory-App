<!DOCTYPE html>
<html lang="en">
<head>
    <meta charset="UTF-8">
    <meta name="viewport" content="width=device-width, initial-scale=1.0">
    <title>List of Books</title>
</head>
<body>
    <h1>List of Books</h1>
    <ul>
        <% books.forEach(book => { %>
            <li>
                <h2><%= book.title %></h2>
                <p>Genre: <%= book.genre %></p>
                <p>Year: <%= book.year %></p>
                <p>Author: <%= book.author.firstName %> <%= book.author.lastName %></p>
                <% if (book.coverImage) { %>
                    <img src="/uploads/<%= book.coverImage %>" alt="Cover Image" width="150">
                <% } %>
            </li>
        <% }) %>
    </ul>
</body>
</html>
